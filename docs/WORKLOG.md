# Worklog

Append-only session journal, newest entry first. Every agent session ends by adding an entry.

Template:
```
## YYYY-MM-DD — short title
**Done:** what actually changed (files, decisions, code)
**Learned:** anything non-obvious discovered
**Next:** the single most useful next step
```

---

## 2026-08-08 (cont.) — ADR-0025: counterfactual soundness (TB-040, the false time-lock)
**Done:** User caught the trap in the KNOWLEDGE_REQUIREMENT collapse: works have hidden dependencies — Einstein's papers could have been written in 2000 BC if their inputs existed, so dated works must never gate possibility ("could Rome have steam engines? Maybe — but not if we depend on an 1800 paper for no reason"). ADR-0025 accepted: two date computations (actual = Did-Happen layer, full participation; earliest-possible = necessary layer only, traversal masks out WORK_PUBLICATION and BIOLOGICAL_ENTITY); linter flags ENABLES-from-work; person anachronism checks validate recorded history but never constrain counterfactuals; counterfactuals return gap lists ("missing: precision boring, pressure-vessel metallurgy"), never contingent dates. TB-040 added+Solved; ARCHITECTURE signature queries updated; Q-21 collapse note refined. Also: user recalled Church-vs-science was the ORIGINAL motivation for inhibits — suppression is core content ("show the mess"); noted in Q-21 as strengthening the two-partition lean.
**Learned:** Conflating necessity and contingency silently poisons every "what if" query at scale — the prime directive applied to modality: "impossible before 1800" (wrong) vs "not realized until 1800, possible earlier given X" (true).
**Next:** User's one-vs-two story-partition ruling → close Q-21 (final basis + migration table); then Q-02.

## 2026-08-08 (cont.) — ADR-0025 amended: computed-never-authored + debug harness
**Done:** User clarified two properties of earliest-possible, added to ADR-0025: (5) it is computed fresh per query from the graph at hand, never stored/authored (same family as ADR-0002 state-as-query — can't go stale, improves with the graph); (6) the counterfactual query IS the graph's debug harness — ask, trace why/why-not, judge the trace; absurd answers are diagnoses localizing missing constraints/edges (historically: the vacuum-tube iPhone trace is what revealed the need for constraints), feeding the flag/bounty loop. The graph tests itself through use.
**Next:** unchanged — story-partition ruling, then Q-21 close, then Q-02.

## 2026-08-08 (cont.) — ADR-0026: the facts-only store (the master axiom)
**Done:** User stated the hard lock: ONLY write down facts; everything else is solved. ADR-0026 accepted: the store is a fact base (ground facts, citable); everything modal/hypothetical/aggregate is entailment computed at query time (the extensional/intensional split). Schema litmus test: "could someone cite a source for this value, or is it an answer the graph should produce?" — answers never become fields. Caches permitted but never authoritative (recomputation always wins). Subjective editorial data stays legal as facts-about-human-assessment, labeled. Unifies ADR-0002/0019/0025's local derived-not-stored rules into one axiom; joins ADR-0015 and ADR-0023 as the third standing screen on all future schema changes. Glossary: ground fact, entailment. Audit task noted: current_state/active_instance_count must be marked non-authoritative or removed at schema lock.
**Learned:** Stored inference is future wrong information by time bomb — it goes stale invisibly at scale. The user's Bill Gates example is the cleanest statement: "released 2007" is written; "could Gates have built it" is asked.
**Next:** story-partition ruling → Q-21 close → Q-02 → schema-lock field audit (now includes the ADR-0026 screen).

## 2026-08-08 (cont.) — Q-02 and Q-21 CLOSED via direct questions (ADR-0027, ADR-0028)
**Done:** Asked the user the four pending questions directly. Rulings: (1) truth = two orthogonal axes, validity on nodes AND edges, epistemic on edges → ADR-0027, Q-02 Resolved; (2) story partition: user's answer "this is a tech tree first and foremost — WWII is only there because it leads to technology" settled it as ONE ASSOCIATION partition (lazy abstraction applied to the schema; split-by-qualifier is a costless mechanical migration later) → ADR-0028 with final 8-type basis + full legacy→basis+qualifier migration table, Q-21 Resolved; Node.cpp EdgeType enum rewritten to the basis, `qualifier` field added to DependencyEdge; tech-first principle added to VISION. (3) Field audit: user wants discussion first — their FTL/locked concern and the "ask what a not-yet-invented tech would depend on, then invent it" use case (invention-discovery = the counterfactual harness pointed at the future). NOT removed yet; discussion in chat.
**Learned:** The user's uncertainty about events was itself the ruling: don't build structure for second-class content prematurely.
**Next:** settle the field-audit discussion (state fields) — the LAST item before Phase 1 schema lock. Then linter rules + qualifier vocabulary as carried-forward tasks.

## 2026-08-08 (cont.) — Field audit complete; math/conditional knowledge as killer feature (TB-041)
**Done:** User ratified computed state via the FTL/superconductor discussion ("if we unlock that one node it unlocks everything underneath" — with the correction that the payoff set is the node's CHILDREN). Removed current_state and active_instance_count from Node.cpp (ADR-0026 audit complete; NodeState enum kept as solver output vocabulary). TB-041 added+Solved: RH's child subtree ("look what falls if it's false" = impact analysis on a conjecture); conditionality is COMPUTED (any proof path avoiding hypothetical nodes → unconditional — ADR-0017 OR + ADR-0026); hypothetical 0-parent leaves with pre-built child trees; **new rule caught: magic-box-REALIZED default applies only to validity=current_truth — hypothetical validity blocks realization regardless of parents (false-unlock guard)**. VISION goal 6 added (invisible dependency structure of knowledge: RH children, Galois bridges, invention prospecting). ARCHITECTURE signature queries + state note updated.
**Learned:** Mathematics is a third audience with the exact dependency structure the graph serves, and "is this theorem conditional on RH" — tracked by hand in surveys today — becomes an entailment. User flags this as very important.
**Next:** Phase 1 remaining: v1 schema write-up + linter rules + qualifier vocab + partial TB cases (TB-011 Saturn V, TB-029, TB-031). Schema fields themselves are now LOCKED.

## 2026-08-08 (cont.) — TB-042: math evolution thought experiments all resolve into existing machinery
**Done:** User's three thought experiments captured as TB-042, each solved by composition: (a) new RH-free proof = OR branch to the proof's concept inputs → conditionality recomputes for the whole descendant subtree instantly, purely additively; (b) disproven Jacobian + Weak Jacobian salvage = ordinary new node + check-queue triage ("which children survive on the weak form" reuses the Q-04 LLM pipeline verbatim); (c) proven-true theorem with no valid support path = UNREALIZED — the entailment-vs-fact contradiction is an automated missing-node detector, identical to the "iPhone has no path through battery" bounty. ARCHITECTURE diagnostics updated.
**Learned:** The moderation pipeline and the mathematics-evolution pipeline are the same machine; the bounty mechanic doubles as a gap-localizing theorem tool. Zero new machinery needed for any of the three — the schema is holding under domain transfer.
**Next:** unchanged — Phase 1 write-up tasks; schema locked.

## 2026-08-08 (cont.) — TEST BED FULLY GREEN: last three Partials resolved by user rulings
**Done:** (1) TB-011 Saturn V: docs-status is fact, buildability is computed with TWO honest answers — artifact (unbuildable, gap list of dead suppliers) vs paradigm (buildable with substitutions; sameness refused per ADR-0022); unbuildable-with-no-traceable-why = missing nodes = bounty. (2) TB-029: substitutability test adopted as human judgment call, default 99.9% NO, wizard makes direct person links high-friction. (3) TB-031: the **operation test** — "require to operate" (ENABLES; nature gets full RegionalAvailability + timelines) vs "why it arrived in this timeline" (ASSOCIATION, solver-invisible); user's needle/vaccine and Windmill-Explosion-of-2164 examples; generalizes ADR-0025's contingency mask to ALL historical causation. (4) Phase 2 seed = the iPhone all the way up (one honest thread; organically hits density test and math). ROADMAP updated. **42/42 test cases Solved.**
**Learned:** The user's edge intuitions consistently reduce to two questions: "is this necessary or merely historical?" and "which node are you actually asking about?" Both are now named tests (operation test, substitutability test).
**Next:** Phase 1 close-out write-ups (v1 schema doc, linter table, qualifier vocabulary) — no open design questions remain.

## 2026-08-08 — ADR-0029 agent-first/MCP; backend research launched (Q-17 active)
**Done:** User pivot: flesh out the backend before authoring; Neo4j decision predates requirements and the agent reality. ADR-0029 accepted: MCP is a first-class API surface (edit verbs, existence search, triage exposed as MCP tools from the first prototype; human UI is a client of the same surface); write path designed for parallel agent swarms into shadow branches with machine-readable rejections; humans are the verification bottleneck BY DESIGN; agents carry identity/model/provenance and earn reputation under the same blast-radius rules as humans. Q-17 updated with the full requirements matrix (R1-R10 derived from ADRs). Launched three background research agents (2026 landscape, live web): graph engines; versioned/branching stores incl. the native-branching-vs-app-level-changesets question; MCP/ANN/GraphRAG stack incl. documented LLM-KG failure modes. Findings will land in docs/research/.
**Learned:** Agent-first inverts the economics: generation cheap and parallel, verification scarce — the moderation design (shadow branches + queues) was already shaped as an agent-scale ingest funnel.
**Next:** synthesize research into a backend recommendation (successor to ADR-0010); then Phase 1 close-out docs in parallel.

## 2026-08-08 (cont.) — ADR-0030: citation-first, never citation-gated (TB-043)
**Done:** User directive: citations first-class; missing citation NEVER rejects a submission (kills usability) but MUST instantly show loud red [needs citation]; repair is a game. ADR-0030 accepted: badge is computed from empty citations (ADR-0026 — self-clearing, never stale); per-claim granularity; red = standing bounty joining the existing bounty loops; cited-only filtered views allowed but default shows the honest mess; MCP reads carry citation status, attach_citation is a first-class verb; reputation rewards citation repair. Directly mitigates the research-documented "plausibility trap" at agent-swarm scale — agents can flood proposals but nothing LOOKS trustworthy until sourcing catches up. TB-043 added+Solved. First research report landed (agent/MCP/vector stack — see docs/research/); two agents still running (graph engines, versioned stores).
**Next:** synthesize backend recommendation when remaining research lands.

## 2026-08-08 (cont.) — Backend research synthesized; ADR-0031 PROPOSED (Postgres-first)
**Done:** All three research streams landed and CONVERGED: the eliminating requirements (versioning, branching, commutative parallel writes, swappability) are app-level in every scalable engine, so pick the store where that layer is cheapest → Postgres. Wikidata/OSM precedent unanimous for app-level changesets; in-memory-solver split neutralizes graph engines' traversal edge; 2025-26 market churn (Kuzu archived by Apple! TigerGraph distressed, Dgraph resold, ArangoDB relicensed) vindicates R9. Synthesis in docs/research/2026-08-backend-synthesis.md. ADR-0031 written as PROPOSED (awaiting user): Postgres system of record (append-only assertions, edges physically partitioned by the 8 basis types, pgvector), app-level ChangeRequests with commutative set-union apply, thin verb-shaped MCP server, canonical JSONL fact-log export as the real truth. ADR-0010 marked superseded-in-part (solver split survives). Watch list with triggers: FlureeDB (native branching, GA 6/2026), DoltgreSQL, LadybugDB (solver substrate), Milvus/Turbopuffer (vector scale jump).
**Learned:** "The engine only needs cheap fat subgraph reads" turns the biggest apparent requirement (graph traversal) into a non-requirement — the old ADR-0010 solver insight is what makes boring-Postgres viable.
**Next:** user ratifies/amends ADR-0031 → then v1 schema doc (now incl. table sketch + JSONL format), linter table, qualifier vocabulary.

## 2026-08-08 (cont.) — ADR-0032 verification ladder; Postgres 100M answer; Neo4j reasons made specific
**Done:** User sketched a 5-level acceptance ladder and delegated fleshing out. ADR-0032 accepted: L1 uncited(red) → L2 cited-unchecked → L3 machine-verified (LLM fetched source, confirmed support, run recorded — catches hallucinated citations at pennies) → L4 human-verified → L5 community-ratified (N reputation-weighted; PROTECTED not frozen — demotion outranks locks). Levels computed from event-facts (never stored/stale; retractions auto-demote everything downstream). Ladder climbs on verification evidence, not submitter species (provenance is separate metadata — refines user's human-vs-LLM rungs). Filters L3+/L4+/L5; honest naming (verification depth, never "100% true"). TB-044 added (hallucinated citation + retracted source). Answered user's questions in chat: Postgres at 100M nodes = routine (Wikidata runs 115M items on MariaDB; caveat pgvector sidecar jump at ~50M vectors); Neo4j reasons made specific (no advantage where we need one, advantage we don't consume, GPLv3-single-instance→enterprise cost cliff at success, single-leader writes vs swarms, reversible via fact log).
**Next:** ADR-0031 ratification still pending (user probing, not yet ruled).

## 2026-08-08 (cont.) — ADR-0033: continuous confidence alongside the ladder (TB-045)
**Done:** User revised the ladder discussion: 5 was arbitrary; wants continuous 0-100; endorsed the three-quantity separation (verification depth / epistemic confidence / source strength-as-input). ADR-0033 accepted: confidence = versioned open formula over source count/independence/reliability + verification events + challenges, computed never stored; sources become first-class nodes with community reliability assessment (Wikipedia perennial-sources pattern); formula contested as a whole like code, never per-claim; display = bands with number+trace on hover (user ruling — false-precision guard); low-score-on-popular-claim = decomposition bounty (TB-045: "Microsoft created DOS" is genuinely fuzzy — bought 86-DOS from SCP — and splits into high-scoring finer facts). User's stated motivation captured in VISION: research-grade AND fun, one fact base two lenses. Ladder (ADR-0032) stands unchanged as the process dimension.
**Next:** ADR-0031 (Postgres) ratification still pending; then Phase 1 close-out docs (v1 schema incl. tables/JSONL, linter, qualifier vocab, confidence formula v1 sketch).

## 2026-08-08 (cont.) — ADR-0034: record time, as-of views, citable snapshots (TB-046)
**Done:** User: citations must be retrievable "AT THE TIME and also CURRENT" for papers citing the graph. ADR-0034 accepted: record time named as the THIRD time axis (vs historical, vs possibility — never conflated, per the versioned-stores research warning); universal as-of queries re-evaluating the full entailment stack with then-current formula/verifier/embedding versions pinned (this is why everything is versioned); citable snapshot permalinks (date + content-hash, DOI-style); diffable citation exports (as-of + current + structured diff — "papers age visibly, never silently"). Near-zero cost: the append-only facts-only constitution already stores everything; this promotes it to product surface. TB-046 added. Glossary: record time.
**Next:** ADR-0031 ratification STILL pending; Phase 1 close-out docs queue grows (add snapshot permalink scheme to v1 schema doc).

## 2026-08-08 (cont.) — ADR-0031 RATIFIED; traversal concern answered in writing; Q-17 closed
**Done:** User ratified Postgres with one concern: solver/search are deep graph traversal. Answer added as ADR-0031 "Traversal Analysis" section: (1) the skeleton-in-RAM argument — (from,to,type) triples in CSR ≈ 8-16GB for 1B edges, whole causal skeleton in one server's memory at 100M+ edges/sec; Postgres never traverses; (2) targeted extraction = frontier expansion, one indexed batch per hop, physically pruned by the 8-type partitions; the SQL graph-killer (unbounded recursive join) is never issued; (3) whole-graph analytics run over periodic skeleton snapshots (LadybugDB candidate); search was never traversal (ANN/set-intersection/B-trees); (4) escape hatches stand via JSONL fact log. Q-17 → Resolved. ROADMAP Phase 2 updated (build on Postgres directly, no interim format).
**Next:** Phase 1 close-out docs (v1 schema: tables + JSONL fact log + snapshot permalinks; linter table; qualifier vocabulary; confidence formula v1 sketch) — all decisions made, pure write-up. Then Phase 2 build.

## 2026-08-08 (cont.) — STRESS TEST: 17-agent red-team; 0 breaks, 17 hardening rules (ADR-0035)
**Done:** User-requested adversarial stress test run as a workflow: 7 finders on distinct attack dimensions (TESTBED-covered ground disallowed) → dedup (28→26) → 9 skeptic verifiers defending the design. **Verdicts: 0 BREAKS, 17 PARTIAL, 9 HANDLED.** No core bet bent; every gap was a composition seam (two sound mechanisms meeting without a written interaction rule). All 17 missing rules codified as ADR-0035 H1-H17 (method-source supersession demotion; interval temporal semantics; timeline coherence; redirect acyclicity as 4th circuit breaker; the Un-merge verb; merge payload semantics; override re-validation; verifier-discreditation fact type; post-merge breaker recheck; composed-mode solver evaluation; exclusion×expression; shadow×implicit-AND; claim-equivalence classes; L3 injection hardening + reliability-capped lift; formula code-channel governance; operator-level independence; poly-hierarchy composition — taxonomy is now officially a DAG). TESTBED gains section G (TB-047..TB-063 → 63 cases, all Solved). Full adversarial record preserved for future agents in docs/research/stress-test-2026-08/ (README + one file per dimension, complete scenarios + full verifier adjudications); summary in docs/research/2026-08-stress-test-report.md.
**Learned:** The 9 HANDLED refutations are free worked proofs of mechanism composition; the PARTIAL class (composition seams) is exactly what paper-phase stress testing exists to catch before a billion nodes makes them catastrophic.
**Next:** Phase 1 close-out docs (implementation checklists must inherit H1-H17), then Phase 2 build.

## 2026-08-08 (cont.) — H5 extended (user's un-merge flow), ADR-0036 neutrality, full review confirmed
**Done:** User reviewed the stress-test findings (loved them). Three outcomes: (1) H5 extended with the user's un-merge lifecycle — H5a merge-history-as-triage-prior (pre-merge assertions auto-propose recorded homes; only post-merge ones need judgment), H5b verification reset-and-re-climb (rehomed assertions keep citations, shed verification events → ~L2 → machine L3 → bounty re-climb to L4/L5), H5c parking generalized (any common ancestor; NO common ancestor → unplaced-claim bounty attached to the un-merge event, never forced). LCA explained to user incl. no-LCA case. (2) ADR-0036 vantage-point neutrality: rules viewpoint-blind, only facts dictate; equal authoring standing; evidence-computed outcomes (no false balance — fringe gets labeled by its citations, not by decree); moderation adjudicates process never truth; bias claims answered with traces. TB-064 (two schools, one gunpowder). (3) Confirmed complete review of all 26 findings: 17 partials read in full during H-rule extraction; 9 HANDLED adjudications now read end-to-end — report summaries verified accurate (H6 decomposition-laundering refutation especially strong: false syntheses can't be shadowed because micro-facts don't entail the priority claim).
**Next:** Phase 1 close-out docs unchanged.

## 2026-08-08 (cont.) — PHASE 1 CLOSE-OUT: docs/SCHEMA.md written (frozen v1)
**Done:** The canonical close-out document: `docs/SCHEMA.md` — supersedes Node.cpp as schema source of truth. Contents: one-paragraph model; node/edge field tables with governing ADRs (audit removals noted); NodeCategory incl. BRAND + nature-with-regions; taxonomy-as-DAG; 8-type basis + starter qualifier vocabulary; RequirementExpr with all H-rule evaluation semantics; trust events (stored) vs ladder/confidence/badges (computed); three time axes with H2/H3 semantics; sources incl. method sources; Postgres physical sketch (partitioned edges, append-only, CR apply semantics with H4/H9); JSONL fact-log format with example lines; snapshot permalink scheme; confidence formula v1 sketch with mandatory golden evals; full circuit-breaker (B1-B4) + linter (L1-L12) table; MCP surface v1 (14 verbs); Phase 2 implementation checklist inheriting H-rules. CLAUDE.md doc map + ROADMAP updated (Phase 1 complete pending user review).
**Next:** User reads SCHEMA.md → sign-off or amendments → Phase 2 build.

## 2026-08-08 (cont.) — The authoring loop captured in VISION
**Done:** User articulated the end-to-end authoring experience ("add iPhone → lands red → parallel rabbit holes → sources solidify → never shrinks, only grows → hyper-parallelizable"); every beat maps to existing law (0030 land-red, 0023 collide-never, 0032 red-ripens-green, append-only monotone growth). Added as a VISION section in the user's own framing. Noted homepage idea: record-time time-lapse of subtrees blooming red and ripening green — "watching humanity verify its own knowledge in real time."
**Next:** unchanged — SCHEMA.md review pending.

## 2026-08-08 (cont.) — UI STACK DECIDED: the tech tree rendered as a planet (ADR-0044)
**Done:** UI discussion (D1-D6) → user rulings incl. THE RULE (ADR-0043: correct and scalable from the start; "billion scale from the start makes this an actual tool, not a demo" — quoted in the ADR). Street-view-over-billions requirement made precise: map-tile architecture, server-computed aggregate tiles from the in-memory skeleton, client never holds >~100k renderables. Renderer research (live 2026 web) CONFIRMED the MapLibre hypothesis: ADR-0044 accepted — MapLibre GL graph surface over a server-generated vector-tile pyramid (PMTiles bulk + dynamic tiler for fresh edits; the three zoom tiers map 1:1 to map layers), deck.gl interleaved as near-zoom escape hatch, Next.js self-hosted w/ Redis cache + on-demand ISR for millions of crawlable permalinks. Precedents: Map of GitHub (690k nodes as tippecanoe tiles), 2026 MSAGLJS tile-pyramid paper; MapLibre is the only candidate with industrial label collision (the part every graph lib fails). Rejected: Cytoscape (the convenience hack ADR-0043 forbids), Sigma (we'd build the whole streaming layer), cosmos/G6/PixiJS-custom. Three risks owned: Q-22 opened (living-graph incremental re-tiling + layout stability — NO published solution at scale, we own the frontier); coordinate contract = day-one freeze (joins the permanent contracts); near-zoom styling ceiling → prototype early. Q-12 → Resolved.
**Next:** viewer build order per ADR-0044 consequences (coordinate contract spec first), in parallel with the iPhone corridor seeding.

## 2026-08-08 (cont.) — The blame corollary: people vouch, defaults never (66 green)
**Done:** User's rule pinned into ADR-0042 as the blame corollary: "if an agent/person didn't personally vouch for the info, it's not there — we assign blame to people, not defaults." Audit caught the last offender: propose_node's PARAMETER default validity="current_truth" — a default in disguise, vouching on behalf of callers who never decided. Fixed: public surface takes validity=None; unstated stays absent; the node stands UNKNOWN until someone personally vouches (kernel's create_node default explicitly labeled test-scaffolding). Corollary also codified: verb-compiled derived facts are owned by the invoker; Decision picks owned by the resolver; "who said this?" never resolves to "the system assumed." New test proves the full loop: agent proposes Widget without vouching → UNKNOWN; Andrew explicitly asserts validity with justification → SATISFIED; the validity fact's author row reads "andrew" — blame assigned, by name. 50+7+9 = **66 green**.
**Next:** iPhone corridor — the front door is now both honest and accountable.

## 2026-08-08 (cont.) — ADR-0042 build-from-zero + born-complete edges (65 green)
**Done:** User rulings on edge authoring: Q1 yes (initial-condition kwargs — start/end/epistemic/justification on creation verbs, one atomic CR, edges born complete); Q2 "everything defaults to the unknown/uncertified thing. It has to build up from 0, always" → ADR-0042: no field ever defaults to standing — epistemic absent = computed "unassessed" (the old MAINSTREAM_FACT default was a credibility printing press at swarm scale); found+fixed the kernel's own violation: missing validity fell back to current_truth — now caps existence at UNKNOWN with a "validity unassessed" gap, standing earned by explicit fact only (contradiction detector likewise explicit-only); Q3 justification never required, always encouraged. Also implemented the missing creation verbs refine/succeed/associate (with kwargs; registered in service). 4 new tests: born-complete one-call edge (dated at birth prunes 2007 query), unstated epistemic stays absent, validity-builds-from-zero (UNKNOWN until asserted, SAT after), story verbs compile + stay solver-invisible. 50 kernel + 7 backend + 8 server = **65 green**.
**Next:** the front door is fully specified and running; iPhone corridor next.

## 2026-08-08 (cont.) — MCP SERVER SHIPPED: dual-face, credentialed, ticketed (61 green)
**Done:** User ruled the three server questions (ADR-0041): BOTH transports (deployable — "have people attach their Claude Code"; read-side principle recorded: cheap frontier models (user cites OpenAI Luna/Tera) do semantic duty ONLY through compilable-question tools — the LLM never queries a billion nodes); server-stamped per-connection credentials ("this WILL be griefed"); tickets built properly NOW. Implemented: migrations/002_server.sql (identities w/ budgets, decision_tickets, search_receipts, wall_time for budget windows); server/httkserver/service.py (transport-agnostic verb executor: authenticate→budget→kernel-view→compile→ apply-as-CR with server-stamped author | persist Decision as ticket | pass Rejection; resolve_decision validates the choice against the stored legal option set, merges into params, re-executes, stamps resolved_by; open_tickets = the check queue exists); mcp_app.py (MCP SDK 2.0: stdio launcher + streamable-HTTP; 7 tools: search_similar, propose_node, verb, resolve_decision, open_tickets, solve, get_node). 8 service tests green vs live PG: unknown-credential rejected, author stamped server-side (never caller-claimed), budget enforced, gate unskippable (Q-20 rejection without receipt), duplicate→ticket→use_existing resolution provenanced, L11 ticket roundtrip compiles the OR end-to-end (solve returns SATISFIED through MCP-shaped calls), illegal choice rejected (outside legal set), L5 passthrough. 46+7+8 = **61 green**.
**Next:** existence-gate semantic upgrade + viewer remain; but the authoring loop is now LIVE end to end — a Claude Code session can attach and start the iPhone corridor.

## 2026-08-08 (cont.) — COVERAGE AUDIT + TEXTURE VERBS: every field authorable (53 green)
**Done:** Process correction first: user flagged I built the verb layer without discussing ("wait are we discussing first?") — memory saved (discuss-before-build for design-heavy steps); verb layer reframed as straw-man and walked decision-by-decision. Then user requested a coverage audit ("verbs for each situation"); audit against worked examples/ADRs/TESTBED found 12+ gaps incl. two genuine defects: category was frozen in node.create (unreclassifiable — fixed: category is now a correctable field assertion, View.node overlays it) and TB-021's OR(platinum, AND(palladium, heat)) was UNAUTHORABLE via verbs. User ruling: "each schema field 100% needs a way to modify it" → implemented ALL: set_attribute, add_time_segment (H3 overlap flags), date_edge, add_iteration, lift_iteration (the ADR-0018 §4 debt PAID — record→node+dated refine edge+tech edges), rename (dated name_history + alias), add_alias, reclassify (with downstream-linter conflict flags), retract_assertion, mark_shadowed (L8 resolution), add_alternative_bundle (TB-021 authorable), move_assertion/park_assertion (unmerge triage closes; park flags H5c bounty), flag (bounty entry). 10 new tests; 46 kernel + 7 backend = 53 green. VERBS.md updated. ROADMAP gains the standing hardening loop (user): use privately → continuous agent red-teams in dev mode vs the RUNNING system → finds become TESTBED+kernel tests → public only when the esoteric well runs dry.
**Next:** MCP server wrapping verbs 1:1 (surface now complete); user iterating verb-by-verb as issues surface.

## 2026-08-08 (cont.) — VERBS SOLIDIFIED: ADR-0040, docs/VERBS.md, verbs.py (43 total green)
**Done:** User directive: verbs must be SOLVED not LLM-based — LLM for ambiguity only; neither humans nor LLMs can break anything through ignorance. ADR-0040: every verb is a deterministic compiler (view, params) → StagedFacts (valid by construction) | Decision (verb refuses to guess; returns the COMPLETE machine-enumerated legal option set + computed evidence; caller picks; pick is provenanced content) | Rejection (typed rule verdict). Illegal structure unrepresentable: role-named verbs encode direction (add_component(whole, part) — no raw from/to at the public surface; raw = admin god-mode). MCP and human UI wrap the identical functions. docs/VERBS.md = the normative catalog (creation, role-named edges with the deterministic L11 same-role trigger via shared-taxonomy-ancestor detection, intercept with leg-type + TB-067 relocation Decisions, specialize, exclude/widen with H17 ancestor enumeration, merge/unmerge with H5a computed pre-merge homes, evidence verbs, L13 PHYSICAL-needs-citation). kernel/httk/verbs.py implements them; 11 new tests: direction safety, L5/L3/L13 rejections, L11 refuse-then-compile-the-OR, taxonomy-cycle rejection, intercept shadow-never-archive with H12 intact post-verb, exclude only-inherited, widen enumerates {processor, thing} and rejects arm, Möbius blocked at COMPILE time, unmerge triage counts only post-merge content (bookkeeping unions excluded). 36 kernel + 7 backend = 43 green.
**Next:** MCP server = thin 1:1 wrapper over verbs.py against PgFactLog (the surface is now fully specified, so the server is mechanical).

## 2026-08-08 (cont.) — POSTGRES + FACT LOG SHIPPED: 7/7 against live DB (32 total green)
**Done:** Checklist step 1. `backend/`: migrations/001_init.sql (facts table as THE LOG + rebuildable projections: node_identities, edge_identities physically partitioned by the 8 basis types, current_fields keyed to assertion fact_ids per ADR-0038, change_requests, citations with locator); httkdb/factlog.py (ChangeRequest staging + transactional apply: B4/H4 redirect-cycle HARD reject with zero facts written, H9 post-merge B1 recheck that FLAGS-never-reorders, retraction recompute, rebuild_projections replay, kernel-shaped JSONL export); docker-compose (postgres:16-alpine, port 5433 — Docker Desktop booted, container live). **The bridge test is the milestone:** author via Postgres CRs → export_jsonl() → kernel Store.load → realizable() — identical verdicts to pure-kernel authoring (gate world: SAT/SAT via transistor). Also proven live: CR commutativity in the DB (edge-before-nodes order converges — ADR-0023 in SQL), Möbius merge hard-rejected atomically, jointly-cyclic innocent CRs merge-but-flag, retraction reverts to prior claim and kernel agrees, projections rebuild identical from log replay, citation locator ("p. 412, table 3") lands. 7/7 backend + 25/25 kernel = 32 green.
**Next:** checklist step 2 — the MCP server (verb-shaped tools over PgFactLog + kernel solve).

## 2026-08-08 (cont.) — THE KERNEL EXISTS: 25/25 tests green
**Done:** Phase 2 step 0 shipped. `kernel/` — the executable spec of SCHEMA.md: `httk/tri.py` (Kleene three-valued logic, ADR-0037), `httk/store.py` (append-only assertions, identity/assertion split ADR-0038, as-of views ADR-0034, JSONL round-trip, H4 redirect breaker + H5 unmerge), `httk/dates.py` (interval certain-violation semantics H2), `httk/solve.py` (two-axis existence×fitness realizability ADR-0039, expression eval with H11 vacuity/H12 shadow/H13 claim-equivalence, DAG inheritance H17 + EXCLUDE ADR-0019, OPTIMIZES skip ADR-0006, ADR-0025 masks-as-vacuous, TB-041 hypothetical guard, TB-066 UNKNOWN, TB-067 constraints-ride, H3 availability, B1 cycles, TB-042 contradiction detector). 25 tests encoding TB-001/002/021/037/041/042/050/057/058/059/065/066/067/068/069 + H2/H3 + ADR-0023/0025/0034/0038 + JSONL + redirects. **All 25 passed on the first run** — the prose and the algebra agree. Reference rulings pinned in kernel/README (OR best-branch selection, overlap→UNKNOWN, shadowed-constraint provider, logical record clock).
**Next:** Postgres schema + fact-log writer (checklist step 1), then MCP server; kernel fixtures become the production solver's acceptance suite.

## 2026-08-08 (cont.) — REVIEW CLOSED: all five items ratified; kernel is next
**Done:** User ratified #5 (semantics kernel) — the external review is fully digested: #1 three-valued entailment ✓, #2 per-claim citations + locators ✓, #3 identity/assertion invariant ✓, #4 doc authority ✓, #5 kernel-before-viewer ✓, plus bonus ADR-0039 (PHYSICAL/FITNESS) born from the user's attack during the walkthrough. Design phase is COMPLETE: 39 ADRs, 69 TESTBED cases all Solved, four constitutional screens, frozen schema, hardened twice (internal red-team + external review + user attacks).
**Next:** Phase 2 step 0 — the reference semantics kernel. Plan: Python as the *reference* implementation language (clarity-first executable spec; production solver language stays open per Q-16 — the kernel is the semantics contract any production implementation must match, verified by sharing the same test fixtures). Scope: fact append + JSONL log, identity/assertion + as-of resolution, three-valued RequirementExpr evaluation with H11-H13, two-axis constraint checking (ADR-0039), taxonomy inheritance (H17) + overrides (ADR-0019), regional availability (H3), interval dates (H2), cycle detection (B1/B4), shadowing semantics. Test selection: ~20 nastiest TESTBED cases incl. TB-001/002/021/033/034/041/042/050/057/058/059/065/066/067/068/069.

## 2026-08-08 (cont.) — ADR-0039: PHYSICAL vs FITNESS constraint classes (TB-069)
**Done:** During the review walkthrough (user attacked changes one-by-one; #1-#3 all ratified; wire/silver compound edit traced as TB-068), the user caught a real classification error: "cost-prohibitive vs literally can't — two classes." ADR-0039 accepted: PHYSICAL (nature's veto — 90%-silicon transistor doesn't FUNCTION; feeds PROVEN_UNREALIZABLE; citation-required, never the default — impossibility carries the burden of proof) vs FITNESS (purpose's veto — vacuum-tube computers and silver transmission lines WORK, they're just absurd for a purpose; prunes per-query; default class; cost/size-for-purpose always here). Solver output now two-axis × three-valued: existence × fitness-with-reasons. Era-mobility automatic (Napoleon's aluminum: fitness flipped, physics didn't — dated values suffice). Vacuum-tube iPhone verdict upgraded: "physically possible, absurdly unfit: [reasons]" — truer and more fun. SCHEMA constraints row + lattice + L13 linter; constraint example §classes; TB-069.
**Next:** review items #4 (doc authority — applied) and #5 (kernel) summarized for user; then Phase 2 step 0.

## 2026-08-08 (cont.) — External review adopted: ADR-0037/0038, doc authority, dates fixed
**Done:** User's external pre-build review adopted in full. (1) ADR-0037: three-valued entailment — SATISFIED/VIOLATED/UNKNOWN with Kleene composition; supersedes presumed-satisfiable (which was a hand-rolled UNKNOWN rendered as YES — a prime-directive violation at the entailment layer that the stress-test skeptics DEFENDED because they argued inside the prose's framing; meta-lesson recorded); realizability lattice PROVEN/UNKNOWN/PROVEN_NOT with LOCKED/THEORETICAL/REALIZED as UI vocabulary; gap lists = the UNKNOWN set. (2) ADR-0038: assertions first-class (assertion_id = fact-log fact_id; citations/verifications/challenges target assertions, never bare nodes) + identity/assertion split extended to edges + the reference invariant as the FOURTH constitutional screen: "references between graph semantics point to identities; evidence and history point to assertions." (3) Document authority: SCHEMA/ADRs/TESTBED normative, ARCHITECTURE/README derived with conflict headers; ARCHITECTURE's stale Neo4j §3 and Node.cpp-source-of-truth line fixed; README counts fixed. (4) Fabricated future timestamps (08-09/08-10) corrected to 2026-08-08 across 30 files — the repo no longer requires time travel. (5) ROADMAP/SCHEMA §13: semantics kernel + ~20 executable TESTBED tests inserted as Phase 2 step 0 (prose → executable → data). TB-066 and constraint example revised to UNKNOWN semantics.
**Next:** explain the changes to the user one by one for attack (starting with three-valued logic); then Phase 2 step 0.

## 2026-08-08 (cont.) — Full solver trace added to constraint example
**Done:** User asked for the concrete tree path choosing silicon over vacuum for iPhone's power_draw<3W. Wrote the debugger-style trace (the future God-Mode output) into the constraint worked example: demand descends with authored per-seam translations; tube branch dies at the per-gate seam by declared-value certain-violation; silicon enters as the surviving branch's ingredient via the zone-refining modifier stack; package check confirms against candidate CPUs' declared cited values (never gate-count arithmetic — Q-10 stays deferred); 1946 query passes tubes — per-query pruning, no scar. Five honesty notes embedded (authored translations, presumed-satisfiable path to bounties, declared-values-only v1 semantics).
**Next:** SCHEMA.md review still pending.

## 2026-08-08 (cont.) — TB-067: three-way split + constraints ride the claim
**Done:** User probed attribute placement ("<3W — add that to CPU? what if added to Transistor before CPU existed?"). Pinned the three-way split: attribute NAME → registry (deduped by the Q-20-style semantic gate + canonicalization + merge redirects); CONSTRAINT → consumer edge (ADR-0005, never on the provider); VALUES → providers that actually have the property (abstract roles declare nothing). NEW RULE: **constraints ride the claim, not the edge instance** — a shadowed edge's constraints remain active demands evaluated along the covering chain (H12 extension, monotone-safe), and Intercept triages relocation to the specific seam (Q-04 shape); un-relocated is coarse, never wrong; any authoring order converges. Misplaced values need no special rule (unsupported facts die at L3). Constraint example updated; TB-067 added.
**Next:** SCHEMA.md review still pending.

## 2026-08-08 (cont.) — TB-066: the permissive-monotone default (absence semantics for attributes)
**Done:** User asked the load-bearing lazy question: how can constraints handle "literally anything" without CPU carrying a billion specs — what makes lazy possible? Answer pinned as the permissive-monotone default: (1) attribute vocabulary is unbounded interned data, never a schema list (ADR-0004); (2) nodes declare only load-bearing attributes, demand-driven (vacuum-tube kill needed ONE attribute at ONE seam — typical economy); (3) NEW RULE: a constraint referencing an undeclared provider attribute passes as presumed-satisfiable, visibly labeled — never fails (ADR-0015: incompleteness must survive), strictly monotone (declaring only prunes more → ADR-0023 holds); (4) the debug-harness bounty loop is the convergence engine (absurd trace names the missing seam attribute; one fact kills the class). Added to constraint worked example (§Absence), TB-066, SCHEMA constraints row.
**Next:** SCHEMA.md review still pending.

## 2026-08-08 (cont.) — Constraint propagation walked (TB-065, the two-path Logic Gate)
**Done:** User asked how iPhone constraints "bubble up" when Logic Gate gains OR(Transistor, Vacuum Tube). Corrected the mental model: nothing bubbles up — demand flows DOWN translating at each hop (ADR-0005 + Bag of Rules), attributes flow UP composing (ADR-0004), branches die per-query at the colliding seam; pruning is an entailment, never stored (iPhone → transistors, ENIAC-1946 → tubes, same graph, both true). Pinned the three-mechanism preference order: gatekeeper topology (no math) > per-unit seam constraints (no aggregation) > aggregate simulation (needs Q-10 quantities — deferred; design never depends on it). New worked example `docs/examples/constraint-propagation-worked-example.md`; TB-065 added+Solved.
**Next:** SCHEMA.md review still pending.

## 2026-08-08 (cont.) — Intercept walked concretely; two-outcome nuance pinned
**Done:** User asked how adding a node between CPU and transistor works. Walked the full Intercept flow in SCHEMA.md vocabulary (search_similar → propose_node red → one CR: two edges + shadow-mark on the original; iPhone untouched; no level reset; H12 keeps expressions whole; fully order-independent and fractal). Pinned the nuance the question exposed: Intercept has two outcomes by the original edge's truth-status — true-but-coarse → SHADOW (never archive), wrong-target → archive. Glossary entry added.
**Next:** SCHEMA.md review pending.

## 2026-08-08 (cont.) — Q-21 rulings: two collapses, TB-039, one call left
**Done:** User supplied the reasoning behind each remaining edge type; applying their own partition test collapsed two: KNOWLEDGE_REQUIREMENT → ENABLES (the Einstein-in-1000BC time gate is ordinary MAX-over-dependency-dates propagation; "humans have no parents except knowledge" survives as a linter category rule — BIOLOGICAL_ENTITY only receives ENABLES from knowledge/works) and SPECIFIES_STANDARD → ENABLES (standards are STANDARD_UNIT nodes; traversers prune on target category, no edge partition needed). TB-039 added+Solved: demand attaches to the disease not the virus; virus is separately an ingredient (canonical orthogonality demo); lightning-rod "mitigation" is the demand edge read in reverse — no edge needed; inhibits qualifier reserved for suppression. Q-21 updated; one call remains: one story partition or two (INFLUENCES vs ASSOCIATION; WWII fan-out argues two; assistant lean two).
**Learned:** Node categories are the node-side partition key — an edge type is unnecessary whenever the endpoint's category already prunes (the IEEE 754 collapse generalizes).
**Next:** User rules on the story-partition split → finalize Q-21 basis + mapping table; then Q-02.

## 2026-08-08 (cont.) — ADR-0024 revised: the partition test (user's real rationale)
**Done:** User explained WHY orthogonality matters: edge types are the index partition key for billion-node traversal + LLM semantic search. Standing at DuPont with thousands of interleaved ingredient/component edges, the TYPE is what prunes fan-out before reading anything. Revised ADR-0024 same-day: partition test (type ⟺ pruning need or machine behavior; qualifier ⟺ matters only after selection). Rulings: component≠ingredient and type-of≠refinement stay distinct TYPES (selectivity); spin-off/founded/etc. stay qualifiers (succession partitions prune to a handful — qualifier reads are free there; global flavor searches get a secondary index). Straw-man basis now ~8. Q-21 updated with remaining calls (KNOWLEDGE_REQUIREMENT time gate, SPECIFIES_STANDARD, DRIVES_NEED_FOR/INHIBITS).
**Learned:** Both failure directions are real: too many types = overlap/unlearnable; too few = unselective partitions forcing qualifier scans. The basis is exactly the set of distinctions real traversals prune on.
**Next:** Q-21 reduction with user (3 remaining calls) + Q-02 — the final schema-lock session.

## 2026-08-08 (cont.) — ADR-0024: orthogonal edge basis — user vetoed edge-type accretion
**Done:** User rejected SPUN_OFF_FROM-style additions hard: every edge type must be orthogonal, no per-flavor types. ADR-0024 accepted: the orthogonality test (two types coexist ⟺ some machine consumer treats them differently; human-meaning-only differences are a `qualifier` slug on the edge instance); all previously floated candidates (APPLIES_TO, FOUNDED, SPUN_OFF_FROM, PRODUCED_BY, DISCOVERED_USING…) demoted to qualifiers; adding a basis type henceforth requires an ADR demonstrating new machine behavior. Q-21 reframed from collection to REDUCTION pass with straw-man basis ENABLES/PART_OF/SUBTYPE/OPTIMIZES/SUCCEEDS/ASSOCIATION (~20 enum members + ~15 candidates → ~6). TB-038 → Solved (qualifiers, not vocabulary gap). Glossary: qualifier.
**Learned:** This is the same collapse rule as attributes-not-nodes / aliases-not-nodes / name-data-not-nodes, finally applied to edges — the last accretion surface. My earlier Q-21 framing was drifting accretive; the test bed process caught it via user taste check.
**Next:** Q-21 reduction session WITH user (basis judgment calls are theirs: ingredient-vs-component, SUCCEEDS-vs-ASSOCIATION, KNOWLEDGE_REQUIREMENT's time gate). Q-02 can bundle into the same session.

## 2026-08-08 (cont.) — TB-038 Keysight lineage; Q-21 created; "show the mess" into VISION
**Done:** User's Keysight use case (scope → Agilent → HP → founders) added as TB-038: mechanisms fully covered by ADR-0007/0022/0023 composition (parallel Did-Happen/Must-Happen layers from one node, org churn never contaminating the dependency tree), but it surfaced vocabulary gaps — FOUNDED, SPUN_OFF_FROM, dated PRODUCED_BY custody. Created Q-21 (EdgeType reconciliation) as the formal home for all accumulated edge-vocab cargo (~15 pending types incl. APPLIES_TO), plus per-type rules: category compatibility (isValidConnection linter), traversal participation, narrative-layer flag. VISION.md "Truth with texture" strengthened with the user's articulation: show the mess or it isn't faithful; never simplify into a lie for convenience; Keysight→garage as the canonical demo.
**Learned:** User's stated core motivation: faithfulness over convenience is WHY the tool will work. Corporate spin-offs = product-line custody transfers crossing org boundaries — a distinct shape from forks/rebrands, needing only vocabulary.
**Next:** Q-02 (truth systems), then Q-21 (edge reconciliation) — both are bounded schema passes; good candidates to bundle.

## 2026-08-08 (cont.) — ADR-0023: bidirectional navigation + insertion-order independence (TB-037)
**Done:** User's requirement ("Nokia to paper mill, paper mill to phone, add it anywhere and not be wrong") formalized as ADR-0023: (1) direction is meaning not access — every edge indexed/navigable both ways; (2) insertion-order independence pinned as an invariant — any order of true additions converges to the same graph (already implied by stubs/lazy abstraction/additive specialization/lifting; now a promise no future mechanism may break, and a TESTBED-level acceptance criterion); (3) mid-chain duplicate meets heal via search-first + merge redirects — convergence defined up to merges; (4) becomes an executable permutation-test fixture once code exists. TB-037 Solved. ARCHITECTURE updated.
**Learned:** Order-independence was a lucky consequence of existing decisions; naming it converts it into a design constraint like the prime directive.
**Next:** Q-02 (truth systems), then EdgeType reconciliation (APPLIES_TO and friends waiting).

## 2026-08-08 (cont.) — ADR-0022: identity is the causal chain (TB-036, product morphing)
**Done:** User's exercise: verify the arch survives every way a product can morph, incl. Ship of Theseus + rebrand. Worked example `docs/examples/product-morphing-worked-example.md` (Mustang gens 1-7 → Mach-E brand transplant; Twitter→X; Nokia pivots; Polaroid zombie brand; LibreOffice fork; category drift — 7-type mutation taxonomy, all resolving into existing machinery). ADR-0022 accepted: no stored "sameness" — identity is displayed as the dated causal chain; `name_history` (dated names, lazy) added to Node.cpp; brands promote to Brand nodes only on transplant (dated APPLIES_TO edges; never IS_REFINEMENT_OF across a brand jump); family roots may legally degrade into pure identity containers. TB-036 Solved. Glossary: Brand node, identity container.
**Learned:** The trap was ever answering "is it the same thing?" — any stored sameness flag would eventually be wrong (ADR-0015); chains of dated facts cannot be. APPLIES_TO joins the edge-vocabulary backlog for the EdgeType reconciliation pass.
**Next:** Q-02 (truth systems) — still the last schema-touching open question; edge-enum reconciliation right behind it.

## 2026-08-08 (later) — Q-06 resolved: shadowing (ADR-0021), triggered by "do we break iPhone→camera?"
**Done:** User asked exactly what happens to the generic camera edge. Answer made fully concrete (5-step Specialize operation table; the edge is never broken) and the recurring dependency — the subsumption mechanism — was decided: ADR-0021, `shadowed_by` records on still-true covered edges; edit-time/wizard or linter-proposed + human-confirmed; counting queries skip shadows, truth queries don't; shadows re-validate (and edges resurface) when covering edges change; old transitive-reduction batch job becomes the proposing linter; PRIMARY_REFINEMENT redirects kept separate as migration advice. Node.cpp: `shadowed_by_edge_ids` on DependencyEdge. TB-025 → Solved; TB-035 and camera example updated to cite ADR-0021. Q-06 → Resolved.
**Learned:** Three test cases (TB-025/035 + this question) converged on the same mechanism from different directions — good sign it's the right primitive. Residual solver-phase work: formal per-edge-type "fully covers" definition.
**Next:** Q-02 (truth systems). Phase 1 OPEN list is down to Q-02/03/04/19/20 + deferred Q-10.

## 2026-08-08 (late night) — ADR-0020: additive specialization + uneven resolution (TB-035)
**Done:** User's front-camera edge case (role splits when history makes a distinction important; multi-lens depth only where people care). ADR-0020 accepted: specialization is purely additive — sub-roles via IS_TYPE_OF + edges at truth-granularity; the still-true generic edge is NEVER archived (archiving is only for wrong-target edges); sub-family nodes (iPhone Pro) attach sub-line traits without exclusions; uneven resolution is normal and permanent (depth is opt-in, demand-driven; architecture must permit DigiKey-grade depth, never require it). Worked example: `docs/examples/iphone-camera-worked-example.md`. TB-035 Solved. Q-06 urgency raised (generic+specific coexistence now guaranteed → counting queries need subsumption before Phase 2). Wizard implication recorded: "Specialize" is a distinct additive verb from "Intercept".
**Learned:** The scary-looking reconciliation ("history forces a new distinction") costs two adds and zero edits — the prime directive's payoff in action.
**Next:** Q-02 (truth-systems), and Q-06 moved up the priority list.

## 2026-08-08 (night) — ADR-0019: inheritance + exclusion overrides (TB-034)
**Done:** User's edge case (per-iPhone WiFi versions; an instance lacking a family feature; Sony vs Canon sensors) split into: already-solved (per-instance specificity = ADR-0008/0018) and a real gap — absence. ADR-0019 accepted: family edges are inheritable defaults; instances WIDEN (subsumes ADR-0008's RequirementOverride) or EXCLUDE with justification; inherited-unasserted facts are labeled *presumptions* (keeps ADR-0015 intact); contiguous gaps use dated edges (front camera from 2010), scattered gaps use exclusions. Documented the NOT-vs-EXCLUDE trap (requires-absence ≠ does-not-inherit). Node.cpp: InheritanceOverride struct + field. TB-034 added and Solved. Glossary: exclusion override, presumption.
**Learned:** Real history improved the test case: iPhone 1 HAD a rear camera; the front camera (iPhone 4, 2010) is the true dated-edge example.
**Next:** Q-02 (truth-system overlap) — now four truth-adjacent mechanisms to reconcile (validity, epistemic, date uncertainty, presumption).

## 2026-08-08 (evening) — Q-18 resolved: version families via worked 802.11 example
**Done:** Wrote `docs/examples/802-11-worked-example.md` — full real-history 802.11 walkthrough (family root deps incl. FCC ISM deregulation as LEGISLATION; significance-gated version nodes b/a/g/i/n/ac/ax/be with the new dependency earning each; d/e/h/j as iteration records; TSF attaching to the root since 1997; consumer linking rules; TB-033 counterfactual; Thunderbolt/DDR/CPU-fan-out generalization). ADR-0018 accepted: flat significance-gated version stars (never chained — inheritance stays clean), truth-granular feature attachment, record→edge lifting, no nested sub-graphs in storage (family bubble = zoom rendering), fan-out as consumer-edge constraints + capability routers. TB-006/013/014/015/033 → Solved. Q-18 → Resolved. New glossary terms: family root, lifting.
**Learned:** Thunderbolt 4 is the filter's best proof case — a marketing/certification increment that correctly gets an iteration record, not a node. Misjudged node-worthiness is structurally safe in both directions (create-later + lift, or merge + redirect).
**Next:** Q-02 (truth-system overlap: ValidityStatus vs EpistemicStatus vs date uncertainty), then EdgeType enum reconciliation; Phase 2 seed corridor must include Microprocessor at real density (TB-013 empirical check).

## 2026-08-08 (later still) — Q-01 resolved: requirement logic is a boolean expression tree
**Done:** ADR-0017 accepted (user approved AND/OR/NOT tree, trusting judgment on details): expression tree lives on the consumer node with edge-ID leaves; absent tree = AND of all hard edges; NOT included but editorially discouraged (breaks monotonicity that makes incomplete graphs safe); LogicGroup and alternative_path_id both retired. Node.cpp updated (LogicGroup → RequirementExpr, moved before HistoryNode; edge logic field removed). TB-021 → Solved. Q-01 → Resolved. ARCHITECTURE/GLOSSARY/ROADMAP updated.
**Learned:** The wizard's "alternative or additional?" prompt (ADR-0012 Componentize) is the authoring-side guard that keeps the all-AND default honest.
**Next:** Q-18 — hand-work the full 802.11 family example (versions, TSF, consumers, TB-033 lifting rule) to validate the version-family pattern.

## 2026-08-08 (later) — Nature, semantic existence-search, version-scoped features
**Done:** Added TB-031 (nature as dependency — gap found: natural phenomena need RegionalAvailability for buildability), TB-032 (semantic "does it exist?" search at billion scale → Q-20: embed-on-create + ANN + LLM judgment, create-don't-merge asymmetry), TB-033 (WiFi 1-4/5-8 TSF hypothetical = TB-006 GoPro problem generalized; feature presence attaches at whatever granularity exists, lifts record→edge when version nodes appear). User corrected TB-006 to GoPro 12.
**Learned:** User's "graph inside a node" intuition for 802.11 is right as zoom/LOD *rendering*, rejected as *storage* (would create a second structural mechanism; scoped ordinary nodes/edges + viewer collapse achieve it). User scale ambition confirmed: design must not be limited in theory (billions of nodes).
**Next:** Same as before — work Q-01 (logic model) and Q-18 (version families, now with TB-033) with the user.

## 2026-08-08 — Re-scope: tree before solver; test bed established; prime directive named
**Done:** Created `docs/TESTBED.md` with 30 edge cases (TB-001..030) as the permanent design acceptance suite — including new cases from this session: GoPro GPS gap (TB-006), CPU single-node fan-out (TB-013), 802.11/TSF and Thunderbolt version families (TB-014/015), ASML high-fan-in authoring (TB-027). Wrote ADR-0015 ("never wrong, only incomplete" as the prime directive) and ADR-0016 (tree-first build order — supersedes ADR-0014; solver demoted to Phase 4, community correctness layer promoted to core product). Rewrote ROADMAP accordingly. Added Q-17 (storage engine decision reopened — Neo4j choice came from old-LLM era and predates the TB-scale estimate), Q-18 (version families/fan-out), Q-19 (high-fan-in authoring UX). Wired the test bed into CLAUDE.md rules.
**Learned:** User's framing: the solver was always an added feature — the browsable tree + community correctness (LLM triage + voting, citations) IS the product. Scale ambition: tens of millions–billions of nodes, possibly TBs. Design-on-paper-first is deliberate: schema mistakes at that scale are near-irreversible, hence the test bed as the forcing function.
**Next:** Work TESTBED OPEN cases with the user, starting Q-01 (logic model, TB-021) and Q-18 (802.11/Thunderbolt/CPU worked examples).

## 2026-08-08 — Project restart: documentation system built, old chats distilled
**Done:** Created the full doc system (CLAUDE.md agent contract, docs/: VISION, ARCHITECTURE, GLOSSARY, OPEN-QUESTIONS, ROADMAP, WORKLOG, decisions/ with 14 ADRs, archive/). Digested both Gemini design conversations (~10k lines total) into permanent digests under `docs/archive/digests/` — discovered `Chats/Long chat.pdf` (208 pages) is a print-to-PDF duplicate of `gemini-conversation-2026-01-18-16-05-12.md`, so only the two .md files are canonical sources. Extracted 14 settled decisions into ADRs and 16 open questions into OPEN-QUESTIONS.md.
**Learned:** The old chats settled far more than the README recorded — notably Neo4j + C++ solver split (ADR-0010), state-as-query (ADR-0002), the Manufacturing Test / Lazy Split rules (ADR-0004), the Significance Filter (ADR-0009), and the tracer-bullet build order (ADR-0014). One genuine schema conflict surfaced: Node.cpp's three-level LogicGroup vs. the chat's flatter `alternative_path_id` design (Q-01) — this blocks the solver and should be resolved first.
**Next:** Resolve Q-01 (requirement-logic model), then start Phase 1 tracer bullet (ROADMAP).
